{
  "name": "obsidian-markdown-quiz",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "description": "Development tests only; Obsidian runs main.js directly.",
  "scripts": { "test": "node --test tests/*.test.cjs" },
  "devDependencies": { "jsdom": "^26.1.0", "yaml": "^2.8.1" }
}
